<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.venkateshalive.bankapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:largeHeap="true">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EmiCalculator" />
        <activity android:name=".CurrencyConventor" />
        <activity
            android:name=".AllBank"
            android:label="@string/title_activity_all_bank"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ContactDetailes" />
        <activity android:name=".IntrestCalculator"></activity>
    </application>

</manifest>
